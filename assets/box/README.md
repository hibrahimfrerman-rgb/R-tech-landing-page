# UI Box (saved variants)

This folder keeps **working UI bits we removed/changed**, so you can reuse them later without hunting through git history.

## What’s inside

- `header-cta-quote-pill.md` — the old blue capsule “Get a quote” header button variant.

## How to use

Each file includes:

- what it changes
- the exact HTML/CSS edits to restore it

