# Assets

- `assets/images/` → page images
- `assets/icons/` → SVG icons
- `assets/inbox/` → your “transfer box” for new assets

Tip: keep filenames the same as the current placeholders to swap instantly without editing HTML.

